{
  "name": "mediacrawler-app",
  "private": true,
  "version": "0.1.0",
  "description": "MediaCrawler Monorepo - API (Python) + Web (React)",
  "scripts": {
    "dev": "pnpm run \"/^dev:(data-api|crawler|browser|signer|web)$/\"",
    "dev:web": "pnpm --filter mediacrawler-web dev",
    "dev:data-api": "cd Data-API-Service && uv run python -m uvicorn api.main:app --host 0.0.0.0 --port 8080 --reload",
    "dev:crawler": "cd Crawler-Service && uv run python -m uvicorn api.main:app --host 0.0.0.0 --port 8081 --reload",
    "dev:browser": "cd Browser-Service && uv run python -m uvicorn api.main:app --host 0.0.0.0 --port 9500 --reload",
    "dev:signer": "cd Signer-Service && uv run python -m uvicorn main:app --host 0.0.0.0 --port 8082 --reload",
    "dev:api": "pnpm run \"/^dev:(data-api|crawler|browser|signer)$/\"",
    "build": "pnpm --filter mediacrawler-web build",
    "build:web": "pnpm --filter mediacrawler-web build",
    "preview:web": "pnpm --filter mediacrawler-web preview",
    "lint:web": "pnpm --filter mediacrawler-web lint",
    "install:api": "cd Data-API-Service && uv sync && cd ../Crawler-Service && uv sync && cd ../Browser-Service && uv sync && cd ../Signer-Service && uv sync",
    "db:up": "node scripts/mysql-manager.mjs up",
    "db:down": "node scripts/mysql-manager.mjs down",
    "db:reset": "node scripts/mysql-manager.mjs reset",
    "db:logs": "node scripts/mysql-manager.mjs logs",
    "db:wait": "node scripts/mysql-manager.mjs wait",
    "db:status": "node scripts/mysql-manager.mjs status",
    "db:sync-env": "node scripts/mysql-manager.mjs sync-env",
    "db:generate-sql": "node scripts/mysql-manager.mjs generate-sql"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.15.4"
}
